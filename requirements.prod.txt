# Production-only dependencies for the RetentionAI API.
# This file excludes development, testing, and notebook-related packages
# (jupyter, notebook, ipython, pytest, debugpy, matplotlib, etc.)
# that are present in requirements.txt but not needed at inference time.
#
# Versions are pinned to match requirements.txt for reproducibility.

# --- Web framework ---
fastapi==0.140.0
starlette==1.3.1
anyio==4.14.2
h11==0.16.0
uvicorn==0.51.0
click==8.4.2

# --- Data validation ---
pydantic==2.13.4
pydantic-core==2.46.4
typing-extensions==4.16.0
annotated-types==0.8.0

# --- Data processing & machine learning ---
numpy==2.5.1
pandas==3.0.3
scikit-learn==1.9.0
scipy==1.18.0
joblib==1.5.3
threadpoolctl==3.6.0

# --- Utilities (transitive dependencies, pinned for reproducibility) ---
python-dateutil==2.9.0.post0
tzdata==2026.3
idna==3.18
sniffio==1.3.1
six==1.17.0
